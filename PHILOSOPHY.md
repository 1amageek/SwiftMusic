# SwiftMusic Philosophy

## 音楽を流しながら、コードで音楽を変える

SwiftMusicは、Swiftで音楽を宣言し、演奏を聴きながら書き換えるためのライブラリである。主要な利用環境として、専用Editorアプリ **MusicPlaygournd** を想定する。

中心となる体験は「書く → 聴く → 見る → 書き換える」という連続した創作である。利用者がコンパイル結果を取り出し、再生を組み直す手順を通常の制作フローに要求しない。コンパイルや検証は、この体験を実現するための内部処理として扱う。

この文書は今後の設計判断の基準を所有する。[DESIGN.md](DESIGN.md)と配下の設計資料は、現在の実装契約と検証範囲を所有する。本書の目標は実装済み機能の宣言ではない。現在の実装との差分は末尾に明記する。

## SwiftUIに倣った音楽の宣言

| 概念 | SwiftMusicでの意味 |
|---|---|
| `Music` | SwiftUIの`App`に相当する、作品全体の入口 |
| `Sound` | SwiftUIの`View`に相当する、合成・再利用できる音楽の宣言 |
| `body: some Sound` | 自分がどのような音楽要素から構成されるかを表す |
| `SoundBuilder` | 宣言の並び、条件分岐、反復を音楽の構成へまとめる |
| モディファイア | リズム、音高、演奏表現、音色、音声処理、配置を変更する |
| `Track` | SwiftUIの`Group`のように任意で使える、名前付きのまとまり |

`body`は演奏命令を逐次実行する場所ではなく、演奏したい内容を記述する場所である。再評価するだけで再生位置をリセットしたり、音声デバイスを作り直したりしない。

兄弟の宣言は基本的に同時進行する。並行性を表すために毎回`Parallel`を要求しない。時間的に順番を持つ構成や反復は、その意味を明示して表現する。音源ごとの`let kick`や必須の`Track`を用意せず、その場で宣言できることを重視する。

`Sound`は音楽の構成を表すプロトコルであり、音源の具象型は`Sample`や`Synthesizer`が担う。SwiftUIの構文は参考にするが、音楽の時間・持続・接続順序は音楽として定義する。

## 音楽的な書き心地

リズムや音高は、音楽を考えている時の短い表記で書けるようにする。適した値型には`ExpressibleByStringLiteral`を採用し、リテラルのためだけの事前変数や初期化処理を減らす。

次は文字列パターンで音楽を宣言する記述例である。パターンの既定周期は4拍であり、拍子を暗黙に指定するものではない。

```swift
struct Groove: Sound {
    var body: some Sound {
        Sample("kick")
            .rhythm("x ~ x ~")
            .gain(0.9)

        Synthesizer(.saw)
            .notes("C2 Eb2 G2 Bb2")
            .gain(0.5)
    }
}
```

文字列の文法、既定の周期、音名やオクターブの意味は曖昧にせず、公開契約として定義する。文字列リテラルへの適合は、誤記でプロセスを終了させる理由にはならない。Swiftの構文・型の誤りと、音楽パターンの誤りは検出する段階が異なっても、どちらも編集箇所へ診断を返し、現在の演奏を守る。

## 編集に失敗しても、前のリズムが流れ続ける

編集中のコード、準備中の更新、現在の演奏を分けて保持する。新しいコードが入力されたことだけを理由に、演奏中の有効な内容を捨てない。

```text
コードを編集 → 更新候補を準備・検証
                 ├─ 成功 → 音楽的な境界で演奏へ反映
                 └─ 失敗 → Editorに診断を表示
                              ↓
                        現在の演奏を継続
```

更新候補は、演奏に必要な宣言・音源・処理の準備がすべて整ってから採用する。候補の一部だけを現在の演奏へ混ぜない。新しい編集が入った後に古い準備処理が完了しても、古い内容で上書きしない。

基本の切り替え位置は次の小節境界を目指す。実際の反映単位や境界の指定方法は、拍子と再生位置を踏まえてランタイムの契約として定める。更新によって再生時計を先頭へ戻さず、持続音やエフェクトの余韻の扱いも明示する。

更新の失敗は、現在の演奏に対する停止命令でも、無音へ置き換える命令でもない。診断を表示しながら最後に採用された演奏を継続することは、失敗を成功として隠すこととは異なる。初回に有効な演奏がない場合は、未開始と診断を表示する。

この継続保証は編集・更新の失敗に対するものである。利用者による停止や、音声デバイス・再生処理自体の障害は区別して扱う。

## リズムが見えるEditor

MusicPlaygourndでは、コードとリズムの可視化を一体の制作体験として提供する。基本となる左右表示に加え、Playgroundのようなコード内表示や下部表示も可能な構成を目指す。表示位置を音楽モデルの制約にしない。

```text
┌─────────────────────┬────────────────────────┐
│ コード              │ 演奏中のリズム         │
│ Soundとmodifier     │ 発音・休符・長さ       │
│ 診断と更新状態      │ 拍・小節・再生位置     │
└─────────────────────┴────────────────────────┘
```

音と可視化は同じ採用済みの演奏内容と再生位置に基づく。Editorが独自の規則でコードを解釈し直して、実際と異なるリズムを描かない。コードの位置と対応する音楽要素を追跡できるようにする。

更新に失敗した時は、コード上にエラーを示し、リズムViewは実際に鳴っている内容を表示し続ける。準備中の内容をプレビューする場合は、演奏中の内容と区別して表示する。

## 音楽として体系を持つ

エフェクターだけでなく、音楽の構成、時間、音高、演奏表現を第一級の要素として扱う。

| 領域 | 表現したい意味 |
|---|---|
| 楽曲構成 | パート、セクション、反復、展開 |
| 音楽上の時間 | リズム、音価、休符、拍子、スウィング |
| テンポ | BPM、時間に沿ったテンポの変化 |
| 音高・和声 | 音名、調、音階、コード、ボイシング |
| 演奏表現・イベント変換 | 強弱、奏法、音長、移調、アルペジオ |
| 音源・音声処理 | サンプル、シンセシス、エンベロープ、エフェクト |
| ミックス・制御 | レベル、パン、ルーティング、オートメーション |

この分類は機能を整理するためのものであり、すべてをモディファイアにする規則ではない。構成はBuilder、音源は具象型、既存の宣言への変化はモディファイアなど、意味に合う形を選ぶ。

音楽の配置は拍に基づいて表し、BPMによる実時間への対応づけから分離する。同じリズムや音高の構成を、テンポだけ変えて演奏できるようにする。拍子は小節の構造、テンポは時間の進み方として区別する。強弱による演奏表現とミキサーの音量も区別する。

## SwiftMusicとMusicPlaygourndの境界

| 所有する側 | 責務 |
|---|---|
| SwiftMusic | 宣言言語、音楽的な意味、モディファイアの作用、再利用可能な音楽データと診断の契約 |
| MusicPlaygournd | コード編集、変更の検出、診断の提示、リズム表示、再生操作、制作セッションの管理 |
| 再生ランタイム | 演奏時計、音源・エフェクトの実行、採用済み演奏の保持、更新の安全な切り替え |

再生ランタイムをどのpackageやmoduleに配置するかは、実装設計で決める。Editor固有のUIから音楽の意味を独立させ、SwiftMusicは専用Editor以外からも利用できるようにする。

編集、コード評価、検証、リソース準備は、音声を供給する経路を待たせない。音声処理側が新しい候補を待つのではなく、準備済みの更新を受け取る。切り替え時の状態、所有権、処理時間の上限は、再生ランタイムが実装前に契約化する。

## 設計判断の基準と現在地

新しいAPIは「演奏を続けながら変更できるか」「音楽として読み取れるか」「コード・表示・音の意味が一致するか」で判断する。更新失敗後の継続は、型やビルドの成功だけでなく、実際に演奏しながら誤りを入力し、前のリズムと再生位置が継続することで検証する。

現在の実装は、宣言からイベントと音声処理計画を生成する基盤である。実装済みの利用方法は[README.md](README.md)、詳細契約は[SoundCompositionの設計](Sources/SwiftMusic/SoundComposition/DESIGN.md)を参照する。

文字列リテラルと更新候補の検証・採用状態はSwiftMusicが担う。`LiveMusicUpdate.prepare`が基盤の`SoundCompiler`を利用し、`LiveMusicState`が採用済み内容を更新失敗から保護する。これは音声の準備・再生ではなく、音楽データの更新管理である。

[MusicPlaygournd](MusicPlaygournd/DESIGN.md)は別のmacOS packageとして、Swiftコードの評価、対応音源のPCM準備、小節境界での更新、再生中のリズム表示を実装している。対応する音源・処理と上限は同packageの設計を正本とする。パターン呼び出し位置の追跡、発音中の文字の強調、行に沿った波形とミックス音声のスペクトラム表示を備える。ライブのBPM・マスターフィルター・ディレイ・リバーブ操作はコード評価から分離する。任意のSwift式や複数行文字列内の強調、コードで宣言した任意のエフェクトの実行は未実装である。
